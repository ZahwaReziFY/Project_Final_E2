<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Home | CINELOG</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body{ background:#0f172a; color:white }
    .card{ background:#111827; border-radius:12px }
  </style>
</head>

<body>

<nav class="navbar navbar-dark bg-dark px-4">
  <span class="navbar-brand fw-bold">🎬 CINELOG</span>

  <div>
    <a href="/add-movie" class="btn btn-primary btn-sm">+ Tambah Film</a>
    <a href="/api/logout" class="btn btn-danger btn-sm">Logout</a>
  </div>
</nav>

<div class="container mt-4">

  <h4>Daftar Film</h4>

  <div class="row mt-3">

    <% movies.forEach(m => { %>

      <div class="col-md-3 mb-3">
        <div class="card p-3">
          <h5><%= m.title %></h5>
          <small><%= m.year %></small>
          <p>⭐ <%= m.rating %></p>
          <span class="badge bg-info"><%= m.genre_name %></span>
        </div>
      </div>

    <% }) %>

  </div>

</div>
</body>
</html>